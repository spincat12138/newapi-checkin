services:
  checkin:
    image: ${NEWAPI_CHECKIN_IMAGE:-newapi-checkin:local}
    build:
      context: .
    environment:
      TZ: Asia/Shanghai
      TWOCAPTCHA_API_KEY: ${TWOCAPTCHA_API_KEY:-}
    volumes:
      - ./config.yaml:/config/config.yaml:ro
      - ./logs:/logs
    restart: "no"
